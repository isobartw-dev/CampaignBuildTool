	"build_systems":
	[
		{
			"name": "CampaignBuildTool",
			"variants":
			[
				{
					"name": "install package",
					"shell_cmd": "npm i",
					"working_dir": "${file_path:${folders}}"
				},
				{
					"name": "update package",
					"shell_cmd": "npm update",
					"working_dir": "${file_path:${folders}}"
				},
				{
					"name": "init task",
					"shell_cmd": "npm run prepublish",
					"working_dir": "${file_path:${folders}}"
				},
				{
					"name": "runIIS",
					"shell_cmd": "npm run runIIS",
					"working_dir": "$file_path"
				},
				{
					"name": "watch:all",
					"shell_cmd": "npm run watch:all",
					"working_dir": "${file_path:${folders}}"
				},
				{
					"name": "watch:css",
					"shell_cmd": "npm run watch:css",
					"working_dir": "${file_path:${folders}}"
				},
				{
					"name": "watch:image",
					"shell_cmd": "npm run watch:image",
					"working_dir": "${file_path:${folders}}"
				},
				{
					"name": "deploy:all",
					"shell_cmd": "npm run deploy:all",
					"working_dir": "${file_path:${folders}}"
				},
				{
					"name": "deploy:css",
					"shell_cmd": "npm run deploy:css",
					"working_dir": "${file_path:${folders}}"
				},
				{
					"name": "deploy:image",
					"shell_cmd": "npm run deploy:image",
					"working_dir": "${file_path:${folders}}"
				},
				{
					"name": "dev",
					"shell_cmd": "npm run dev",
					"working_dir": "${file_path:${folders}}"
				}
			]
		}
	],
